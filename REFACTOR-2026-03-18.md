# 项目重构记录 - 2026-03-18

## 目标
前后端分离，整理目录结构

## 目录结构变化

### 之前
```
english-roleplay/
├── index.html          # 根目录
├── js/                 # 根目录
├── css/                # 根目录
├── assets/             # 根目录
└── server/             # 后端
    └── *.js
```

### 之后
```
english-roleplay/
├── frontend/           # 前端目录（新建）
│   ├── index.html
│   ├── js/
│   ├── css/
│   ├── assets/
│   ├── package.json    # 前端配置（可选 Vite）
│   └── vite.config.js  # Vite 配置（可选，未使用）
├── server/             # 后端
│   └── *.js
└── docker-compose.yml
```

## 修改的文件

### 1. `server/index-join-ai.js`
```javascript
// 之前
const frontendPath = path.join(__dirname, '..');

// 之后
const frontendPath = path.join(__dirname, '..', 'frontend');
```

### 2. `frontend/index.html`
- 直接在 HTML 中通过 `<script type="module">` 加载所有 JS 模块
- CSS 通过 `<link rel="stylesheet">` 引入
- 不依赖构建工具，简单直接

### 3. 新增文件
- `frontend/.gitignore` - 前端忽略文件
- `.dockerignore` - 更新，忽略 frontend/node_modules

## 使用方法

### 开发模式（简单直接）
```bash
# 启动后端（同时 serve 前端静态文件）
cd server
npm install
npm start

# 访问：http://localhost:3000
```

### 可选：使用 Vite 开发（需要额外配置）
```bash
# 安装前端依赖
cd frontend
npm install

# 启动 Vite 开发服务器（端口 5173，代理 /api 到后端）
npm run dev

# 在另一个终端启动后端
cd server
npm install
node index-join-ai.js
```

### Docker 部署
```bash
docker-compose up -d
```

## 前端模块化

前端 JS 文件使用 ES6 模块，通过 `type="module"` 加载：

```html
<script type="module" src="js/auth-client.js"></script>
<script type="module" src="js/characters.js"></script>
<script type="module" src="js/scenes.js"></script>
<script type="module" src="js/conversations.js"></script>
<script type="module" src="js/startvoicechat-client.js"></script>
<script type="module" src="js/particles.js"></script>
<script type="module" src="js/animations.js"></script>
<script type="module" src="js/app.js"></script>
<script type="module" src="js/demo-features.js"></script>
```

**注意：** 模块加载顺序很重要，依赖关系：
- `app.js` 依赖所有其他模块
- 各功能模块（characters.js, scenes.js 等）将全局对象挂载到 `window`

## 下一步优化建议

1. **TypeScript** - 逐步迁移到 TypeScript 获得类型安全
2. **组件化** - 如果 UI 变复杂，考虑使用 Vue/React
3. **构建优化** - 如需代码分割和压缩，可启用 Vite 构建
4. **API 类型定义** - 前后端共享 API 类型

## 注意事项

- 当前不使用 Vite 构建，直接 serve 静态文件
- 生产环境通过 Docker 部署，自动包含前端文件
- 前端 JS 使用 ES6 模块，现代浏览器直接支持

## 2026-03-25 清理更新

- 🗑️ 删除 `frontend/main.js`（不再使用，仅保留注释说明）
- 🗑️ Vite 构建流程未实际使用，仅作为可选配置保留
- ✅ 实际部署：后端直接 serve `frontend/` 目录静态文件
